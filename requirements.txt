requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
jieba>=0.42.1
scikit-learn>=1.3.0
flask>=3.0.0
lunardate>=0.2.2
python-dateutil>=2.8.2
# production WSGI server (used by Render); harmless to install on Windows even
# though the gunicorn CLI itself only runs on POSIX.
gunicorn>=21.2.0
