# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
env/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Project — generated at build time on the server; safe to omit locally too.
# (Keep `data/` directory itself via .gitkeep so the app can write into it.)
data/news.db
data/raw_html/
data/*.db-journal

# logs
*.log

# Claude Code working metadata — not part of the project
.claude/
CLAUDE.md
memory/
